<project name="2toast launcher" default="dist" basedir=".">
  <description>
    2toast launcher manages the Minecraft client and resources
  </description>
  <property name="src" location="src"/>
  <property name="build" location="bin"/>
  <property name="dist" location="dist"/>
  <property name="lib" location="lib"/>

  <target name="init">
    <tstamp/>
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init" description="Compile the source">
    <javac srcdir="${src}" destdir="${build}" includeantruntime="false">
      <classpath path="libsrc"/>
    </javac>
  </target>

  <target name="dist" depends="compile" description="Generate the dist files">
    <jar jarfile="${dist}/2toast-launcher-${DSTAMP}.jar" basedir="${build}">
      <manifest>
        <attribute name="Main-Class" value="net.minecraft.MinecraftLauncher"/>
      </manifest>
      <zipgroupfileset dir="${lib}"/>
    </jar>
  </target>

  <target name="clean" description="clean up" >
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>
</project>
